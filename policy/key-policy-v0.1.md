# Key Policy v0.1

- Private issuer keys MUST NOT be committed to Git.
- Private issuer keys MUST NOT be published as repository artifacts.
- Production signing MUST use isolated key custody.
- Key rotation is represented by a signed protocol event and a new published verification document.
- This repository contains only public verification material and test code.
