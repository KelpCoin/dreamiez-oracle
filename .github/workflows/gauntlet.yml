name: Dreamiez G0 Gauntlet

on:
  pull_request:
  push:

jobs:
  g0:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
      - name: Run G0 Gauntlet
        run: node gauntlet/run-gauntlet.js
